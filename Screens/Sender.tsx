import React from 'react';
import { Text, StyleSheet } from 'react-native'

const Sender = () => {
  return (
    <Text style={styles.container}> PUTE </Text>
  );
};

const styles = StyleSheet.create({
  container: {
    color: '#fff'
  }
});

export default Sender;